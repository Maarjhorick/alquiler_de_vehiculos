# Perfil de DESARROLLO. Los valores por defecto (despues de ':') solo
# aplican en tu maquina local; en cualquier otro entorno se deben
# sobreescribir con variables de entorno reales.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/alquiler_de_vehiculos?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:paulcars_app}
spring.datasource.password=${DB_PASSWORD:PaulCars_Dev2026!}

# En desarrollo esta bien que Hibernate actualice el esquema solo.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true